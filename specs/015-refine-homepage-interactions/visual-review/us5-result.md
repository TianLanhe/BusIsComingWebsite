# US5 验收：高保真响应式构图

日期：2026-08-25

## 端点结果

- 1440×960、390×844、320×844 的三语 Hero golden 全部通过；共覆盖 5 个故事 × 3 语言 × 3 viewport。
- zh-Hant/en 的 Story 01 已与 Figma 同尺寸 reference 生成 side-by-side、50% overlay、diff 和 SHA 清单。
- zh-Hans 在三个 viewport 通过文本完整、无溢出、无横向滚动、触控目标和几何检查；没有独立 Figma 像素级结论。
- 前景手机四边完整、9:16 固有比例不变，后景保持环形远近与倾斜，故事轨不覆盖手机底边。

## 连续 resize 与方向变化

浏览器自动化按 `1440×960 → 1200×900 → 390×844 → 320×844 → 844×390 → 390×844` 连续改变 viewport，当前故事、语言、路线输入、下载 provider 和 FAQ 展开状态均未重置，也没有额外故事切换。

测试在 844×390 首次发现 FAQ 双栏最小宽度溢出；根因是 820px breakpoint 晚于 `320 + 520 + gap + padding` 的真实需求。只把 FAQ 单栏 breakpoint 调整到 980px 后，横向滚动归零，1440/390/320 已批准构图未变化。

## 其他页面

Privacy 使用真实 Logo、品牌和本地化返回首页入口，随页面自然滚走；Footer 复用真实品牌。路线、下载、FAQ 与 Privacy 的既有任务保持可用。
