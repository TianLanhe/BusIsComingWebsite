# App Screenshots

当前目录包含第一版网站用于首屏轮播的高保真临时 mock。Android 主项目当前未提供可直接复用的真实页面截图，后续实现前应替换为真实 Android App 截图。

素材状态会在 `frontend/src/content/carouselSlides.ts` 中以 `placeholder` 标记，并在页面中展示待替换说明，避免把 mock 伪装成真实截图。
