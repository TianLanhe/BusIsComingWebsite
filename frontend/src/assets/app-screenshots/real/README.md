# 脱敏真实截图资产

本目录只保存从 `app真实截图/` 生成的脱敏副本，前端页面不得直接引用原始截图。

规则：

- 每个功能点按 manifest 中的 `featureId` 分组。
- 原始文件名或序号为 `1` 的图片作为该功能点主图。
- 真实地点、站名、路线号、搜索记录和手机系统无关内容必须被遮蔽或替换。
- 价格、时间和 ETA 数值保留，用于展示真实数据形态。
- 新增或替换截图后，先更新 `manifest.json`，再运行 `npm --prefix frontend run sanitize:screenshots`。
